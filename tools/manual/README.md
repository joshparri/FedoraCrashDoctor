# Manual Diagnostic Experiments

These scripts preserve useful one-off analysis experiments. They are intentionally
kept outside the repository root so standard unittest discovery does not import
and execute them as tests.

Real automated tests belong under `tests/`.
